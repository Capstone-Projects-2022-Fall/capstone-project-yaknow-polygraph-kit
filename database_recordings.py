import mysql

db = mysql.connector.connect(
    host="173.255.232.150",
    user="cis4398",
    passwd="dNC=IK~9)7",
    database="Recordings"
)

#CREATE TABLE `SingularRecording` (
#	`question` TEXT,
#	`response` CHAR,
#	`timestamp` DATETIME,
#	`blood_pressure` DECIMAL,
#	`skin_conductivity` DECIMAL,
#	`respiration_belt` DECIMAL,
#	`pulse` DECIMAL,
#	`id` INT NOT NULL AUTO_INCREMENT
#);

